building_aof_federal_holdings = {
	base_buildtime = @b2_time
	# owner_type = subject_holding
	icon = building_aof_federal_holdings
	planet_limit = 1

	category = trade
	potential = {
		# always = yes
		is_sector_capital = yes
		owner = {
			exists = federation
			federation = {
				has_federation_law = succession_type_election
			}
		}
	}
	building_sets = {
		government
		urban
	}
	allow = {
		custom_tooltip = {
			fail_text = aof_need_permission_to_build
			owner = { check_variable = { which = aof_fed_permit value > 0 } }
		}
	}
	on_queued = {
		owner = { add_aof_fed_permit = { ADD = -1 } }
		# this has access to the variables "local_spent_<resource>"
	}
	
	on_unqueued = {
		owner = { add_aof_fed_permit = { ADD = 1 } }
		# this has access to the variables "local_spent_<resource>"
	}
	destroy_trigger = {
		is_sector_capital = no
		owner = {
			NOT = { exists = federation }
			AND = {
				exists = federation
				federation = { NOT = { has_federation_law = succession_type_election } }
			}
		}
	}
	on_destroy = {
		owner = {
			if = {
				limit = {
					exists = federation
					federation = {
						has_federation_law = succession_type_election
					}
				}
				add_aof_fed_permit = { ADD = 1 }
			}
			else = {
				add_resource = { trade = 15000 }
			}
		}
	}
	# triggered_planet_modifier = {
	# 	potential = {
	# 		exists = owner
	# 		owner = {
	# 			exists = federation
	# 			federation = { federation_level > 2 }
	# 		}
	# 	}
	# 	owner = {
	# 		cohesion_add = -0.5
	# 	}
	# }
# Galactic Union / default_federation
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 2 has_federation_type = default_federation }
			}
		}
		modifier = {
			job_soldier_or_warrior_add = 75
			job_bureaucrat_add = 75
		}
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 3 has_federation_type = default_federation }
			}
		}
		modifier = {
			job_soldier_or_warrior_add = 75
			job_bureaucrat_add = 75
		}
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 4 has_federation_type = default_federation }
			}
		}
		modifier = {
			job_soldier_or_warrior_add = 150
			job_bureaucrat_add = 150
		}
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 5 has_federation_type = default_federation } # only with another mod increasing fed levels
			}
		}
		modifier = {
			job_soldier_or_warrior_add = 300
			job_bureaucrat_add = 300
		}
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 2 has_federation_type = default_federation }
			}
		}
		job_logistics_drone_add = 150
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 3 has_federation_type = default_federation }
			}
		}
		job_logistics_drone_add = 150
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 4 has_federation_type = default_federation }
			}
		}
		job_logistics_drone_add = 300
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 5 has_federation_type = default_federation } # only with another mod increasing fed levels
			}
		}
		job_logistics_drone_add = 600
		
	}
# Trade League / trade_federation
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 2 has_federation_type = trade_federation }
			}
		}
		job_trader_add = 150
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 3 has_federation_type = trade_federation }
			}
		}
		job_trader_add = 150
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 4 has_federation_type = trade_federation }
			}
		}
		job_trader_add = 300
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 5 has_federation_type = trade_federation } # only with another mod increasing fed levels
			}
		}
		job_trader_add = 600
		
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 2 has_federation_type = trade_federation }
			}
		}
		job_logistics_drone_add = 150
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 3 has_federation_type = trade_federation }
			}
		}
		job_logistics_drone_add = 150
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 4 has_federation_type = trade_federation }
			}
		}
		job_logistics_drone_add = 300
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 5 has_federation_type = trade_federation } # only with another mod increasing fed levels
			}
		}
		job_logistics_drone_add = 600
		
	}
# Martial Alliance / military_federation
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 2 has_federation_type = military_federation }
			}
		}
		modifier = {
			job_soldier_or_warrior_add = 150
		}
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 3 has_federation_type = military_federation }
			}
		}
		modifier = {
			job_soldier_or_warrior_add = 150
		}
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 4 has_federation_type = military_federation }
			}
		}
		job_soldier_or_warrior_add = 300
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 5 has_federation_type = military_federation } # only with another mod increasing fed levels
			}
		}
		job_soldier_or_warrior_add = 600
		
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 2 has_federation_type = military_federation }
			}
		}
		job_logistics_drone_add = 150
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 3 has_federation_type = military_federation }
			}
		}
		job_logistics_drone_add = 150
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 4 has_federation_type = military_federation }
			}
		}
		job_logistics_drone_add = 300
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 5 has_federation_type = military_federation } # only with another mod increasing fed levels
			}
		}
		job_logistics_drone_add = 600
		
	}
# Research Alliance / research_federation
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 2 has_federation_type = research_federation }
			}
		}
		job_archaeoengineers_add = 150
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 3 has_federation_type = research_federation }
			}
		}
		job_archaeoengineers_add = 150
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 4 has_federation_type = research_federation }
			}
		}
		job_archaeoengineers_add = 300
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 5 has_federation_type = research_federation } # only with another mod increasing fed levels
			}
		}
		job_archaeoengineers_add = 600
		
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 2 has_federation_type = research_federation }
			}
		}
		job_archaeo_drone_add = 150
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 3 has_federation_type = research_federation }
			}
		}
		job_archaeo_drone_add = 150
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 4 has_federation_type = research_federation }
			}
		}
		job_archaeo_drone_add = 300
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 5 has_federation_type = research_federation } # only with another mod increasing fed levels
			}
		}
		job_archaeo_drone_add = 600
		
	}
# Holy Covenant / spiritualist_federation
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 2 has_federation_type = spiritualist_federation }
			}
		}
		job_archaeoengineers_add = 150
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 3 has_federation_type = spiritualist_federation }
			}
		}
		job_archaeoengineers_add = 150
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 4 has_federation_type = spiritualist_federation }
			}
		}
		job_archaeoengineers_add = 300
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 5 has_federation_type = spiritualist_federation } # only with another mod increasing fed levels
			}
		}
		job_archaeoengineers_add = 600
		
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 2 has_federation_type = spiritualist_federation }
			}
		}
		job_archaeo_drone_add = 150
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 3 has_federation_type = spiritualist_federation }
			}
		}
		job_archaeo_drone_add = 150
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 4 has_federation_type = spiritualist_federation }
			}
		}
		job_archaeo_drone_add = 300
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 5 has_federation_type = spiritualist_federation } # only with another mod increasing fed levels
			}
		}
		job_archaeo_drone_add = 600
		
	}
# Hegemony / hegemony_federation
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 2 has_federation_type = hegemony_federation }
			}
		}
		job_politician_add = 75
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 3 has_federation_type = hegemony_federation }
			}
		}
		job_politician_add = 75
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 4 has_federation_type = hegemony_federation }
			}
		}
		job_politician_add = 150
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = no
				exists = federation
				federation = { federation_level > 5 has_federation_type = hegemony_federation } # only with another mod increasing fed levels
			}
		}
		job_politician_add = 300
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 2 has_federation_type = hegemony_federation }
			}
		}
		job_observator_drone_add = 75
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 3 has_federation_type = hegemony_federation }
			}
		}
		job_observator_drone_add = 75
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 4 has_federation_type = hegemony_federation }
			}
		}
		job_observator_drone_add = 150
	}
	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_gestalt = yes
				exists = federation
				federation = { federation_level > 5 has_federation_type = hegemony_federation } # only with another mod increasing fed levels
			}
		}
		job_observator_drone_add = 300
	}

	resources = {
		category = planet_holdings
		cost = {
			minerals = @b1_minerals
			trade = 1000
			alloys = 250
		}
		upkeep = {
			energy = @b1_upkeep
			unity = 15
		}
	}
	ai_weight_coefficient = 1000
}
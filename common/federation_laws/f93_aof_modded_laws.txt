@cooldown = 3600
@cohesion_loss = -20

assembly_high = {
	potential = {}
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = assembly_high }
					has_modifier = federation_law_category_assembly_cooldown
					has_any_federation_law_in_category = law_category_assembly
				}
			}
		}
	}
	modifier = {}
	on_enact = {
		add_cohesion = @cohesion_loss
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_law_category_assembly_cooldown
				days = @cooldown
			}
		}
	}
	ai_weight = { 
		base = 0
		
		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 41
		}
	}
}
assembly_grand = {
	potential = {}
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = assembly_grand }
					has_modifier = federation_law_category_assembly_cooldown
					has_any_federation_law_in_category = law_category_assembly
				}
			}
		}
	}
	modifier = {
		federation_experience_add = 3
	}
	on_enact = {
		if = {
			limit = { NOT = { has_federation_flag = had_grand_assembly } }
			every_member = {
				country_event = { id = f93_aof_election.302 }
			}
			set_federation_flag = had_grand_assembly
		}
		add_cohesion = @cohesion_loss
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_law_category_assembly_cooldown
				days = @cooldown
			}
		}
	}
		ai_weight = {
		base = 0

		# AI will always accept, but not suggest
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 31
		}
	}
}

diplomatic_bloc_default = {
	potential = {}
	allow = {
		has_federation_law = vote_weight_equal
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = diplomatic_bloc_default }
					has_modifier = federation_law_category_diplomatic_block_cooldown
					has_any_federation_law_in_category = law_category_diplomatic_block
				}
			}
		}
	}
	modifier = {
		federation_leader_diplo_weight_contribution_add = 0.1
	}
	on_enact = {
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_law_category_diplomatic_block_cooldown
				days = @cooldown
			}
		}
	}
	ai_weight = { 
		base = 0
		
		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 41
		}
	}
}
diplomatic_bloc_common_voice = {
	potential = {}
	allow = {
		has_federation_law = vote_weight_equal
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = diplomatic_bloc_common_voice }
					has_modifier = federation_law_category_diplomatic_block_cooldown
					has_any_federation_law_in_category = law_category_diplomatic_block
				}
			}
		}
	}
	modifier = {
		federation_leader_diplo_weight_contribution_add = 0.2
	}
	required_centralization = {
		base = 3
	}
	cohesion_growth = -1
	on_enact = {
		add_cohesion = @cohesion_loss
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_law_category_diplomatic_block_cooldown
				days = @cooldown
			}
		}
	}
	ai_weight = { 
		base = 0
		
		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 31
		}
	}
}
diplomatic_bloc_clear_voice = {
	potential = {}
	allow = {
		has_federation_law = vote_weight_equal
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = diplomatic_bloc_clear_voice }
					has_modifier = federation_law_category_diplomatic_block_cooldown
					has_any_federation_law_in_category = law_category_diplomatic_block
				}
			}
		}
	}
	required_centralization = {
		base = 4
	}
	modifier = {
		federation_experience_add = 1
		federation_leader_diplo_weight_contribution_add = 0.3
	}
	on_enact = {
		add_cohesion = @cohesion_loss
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_law_category_diplomatic_block_cooldown
				days = @cooldown
			}
		}
	}
	ai_weight = { 
		base = 0
		
		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 21
		}
	}
}
diplomatic_bloc_united = {
	potential = {}
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = diplomatic_bloc_united }
					has_modifier = federation_law_category_diplomatic_block_cooldown
					has_any_federation_law_in_category = law_category_diplomatic_block
				}
			}
		}
	}
	required_centralization = {
		base = 5
	}
	modifier = {
		federation_leader_diplo_weight_contribution_add = 0.5
	}
	on_enact = {
		add_cohesion = @cohesion_loss
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_law_category_diplomatic_block_cooldown
				days = @cooldown
			}
		}
	}
	ai_weight = { 
		base = 0
		
		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 11
		}

		modifier = {
			desc = f93_aof_civic_shared_burden
			add = 1000
			has_civic = civic_shared_burden
		}
	}
}

fed_accords_coop = {
	potential = {}
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = fed_accords_coop }
					has_modifier = federation_law_category_aof_accords_cooldown
					has_any_federation_law_in_category = law_category_aof_accords
				}
			}
		}
		# custom_tooltip = {
			has_federation_law = treaties_separate_no
		# 	fail_text = "Requires Internal treaties"
		# }
	}
	modifier = {
		federation_research_sharing_mult = 0.1
		federation_experience_add = 4
	}
	required_centralization = {
		base = 4
	}
	on_enact = {
		every_member = {
			remove_modifier = f93_aof_accord_unyielding
			add_modifier = { modifier = f93_aof_accord_coop mult = 1 }
		}
		add_cohesion = @cohesion_loss
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_law_category_aof_accords_cooldown
				days = @cooldown
			}
		}
	}
	ai_weight = { 
		base = 0
		
		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 20
		}
		modifier = {
			desc = federation_acceptance_materialist
			add = 20
			is_materialist = yes
		}
	}
}
fed_accords_unyielding = {
	potential = {}
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = fed_accords_unyielding }
					has_modifier = federation_law_category_aof_accords_cooldown
					has_any_federation_law_in_category = law_category_aof_accords
				}
			}
		}
		has_federation_law = treaties_separate_ethical_partners
	}
	required_centralization = {
		base = 4
	}
	on_enact = {
		every_member = {
			remove_modifier = f93_aof_accord_coop
			add_modifier = { modifier = f93_aof_accord_unyielding mult = 1 }
		}
		add_cohesion = @cohesion_loss
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_law_category_aof_accords_cooldown
				days = @cooldown
			}
		}
	}
	ai_weight = { 
		base = 0
		
		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 20
		}
		modifier = {
			desc = federation_acceptance_militarist
			add = 20
			is_militarist = yes
		}
	}
}
fed_accords_none = {
	potential = {}
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = fed_accords_none }
					has_modifier = federation_law_category_aof_accords_cooldown
					has_any_federation_law_in_category = law_category_aof_accords
				}
			}
		}
	}
	
	on_enact = {
		every_member = {
			remove_modifier = f93_aof_accord_unyielding
			remove_modifier = f93_aof_accord_coop
		}
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_law_category_aof_accords_cooldown
				days = @cooldown
			}
		}
	}
	ai_weight = { 
		base = 0
		
		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 30
		}
	}
}
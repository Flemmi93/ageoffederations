@cooldown = 3600
@cohesion_loss = -20

fed_market_none = {
	potential = {
		NOR = {
			has_federation_law = fed_market_basic
			has_federation_law = fed_market_unity
			has_federation_law = fed_market_military
			has_federation_law = fed_market_strategic
		}
	}
	ai_weight = {
		base = -10
	}
}
fed_market_basic = {
	potential = {}
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = fed_market_basic }
					has_modifier = federation_law_category_aof_market_cooldown
					has_any_federation_law_in_category = law_category_aof_market
				}
			}
		}
		custom_tooltip = {
			has_federation_flag = federation_one_enabled
			fail_text = no_federation_one
		}
	}
	required_centralization = {
		base = 2
	}
	modifier = {}
	on_enact = {
		add_cohesion = @cohesion_loss
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_law_category_aof_market_cooldown
				days = @cooldown
			}
		}
		every_member = {
			remove_market_modifier = yes
			add_modifier = {
				modifier = f93_aof_market_basic
				mult = value:f93_market_policy_basic
			}
			country_event = { id = f93_aof_market.1 }
		}
	}
	ai_weight = { 
		base = 0
		
		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 21
		}
		modifier = {
			desc = federation_acceptance_peaceful_traders
			add = 20
			has_ai_personality = peaceful_traders
		}
		modifier = {
			desc = federation_acceptance_materialist
			add = 5
			is_materialist = yes
		}
	}
}
fed_market_unity = {
	potential = {}
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = fed_market_unity }
					has_modifier = federation_law_category_aof_market_cooldown
					has_any_federation_law_in_category = law_category_aof_market
				}
			}
		}
		custom_tooltip = {
			has_federation_flag = federation_one_enabled
			fail_text = no_federation_one
		}
		OR = {
			has_federation_type = spiritualist_federation
			has_federation_type = default_federation
			custom_tooltip = {
				fail_text = has_fed_cent_high_or_higher_and_internal_treaties
				OR = {
					has_federation_law = centralization_high
					has_federation_law = centralization_very_high
				}
				has_federation_law = treaties_separate_no
			}
		}
		# OR = {
		# 	has_federation_type = spiritualist_federation
		# 	custom_tooltip = {
		# 		fail_text = has_fed_cent_med_or_higher_and_default_federation
		# 		has_federation_type = default_federation
		# 		OR = {
		# 			has_federation_law = centralization_medium
		# 			has_federation_law = centralization_high
		# 			has_federation_law = centralization_very_high
		# 		}
		# 	}
		# 	custom_tooltip = {
		# 		fail_text = has_fed_cent_high_or_higher_and_internal_treaties
		# 		OR = {
		# 			has_federation_law = centralization_high
		# 			has_federation_law = centralization_very_high
		# 		}
		# 		has_federation_law = treaties_separate_no
		# 	}
		# }
	}
	required_centralization = {
		base = 2
	}
	modifier = {}
	on_enact = {
		add_cohesion = @cohesion_loss
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_law_category_aof_market_cooldown
				days = @cooldown
			}
		}
		every_member = {
			remove_market_modifier = yes
			add_modifier = {
				modifier = f93_aof_market_unity
				mult = value:f93_market_policy_unity
			}
			country_event = { id = f93_aof_market.1 }
		}
	}
	ai_weight = { 
		base = 0
		
		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 31
		}
		modifier = {
			desc = federation_acceptance_pacifist
			add = 20
			is_pacifist = yes
		}
		modifier = {
			desc = federation_acceptance_egalitarian
			add = 10
			is_egalitarian = yes
		}
		modifier = {
			desc = federation_acceptance_peaceful_traders
			add = 10
			has_ai_personality = peaceful_traders
		}
	}
}
fed_market_strategic = {
	potential = {}
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = fed_market_strategic }
					has_modifier = federation_law_category_aof_market_cooldown
					has_any_federation_law_in_category = law_category_aof_market
				}
			}
		}
		custom_tooltip = {
			has_federation_flag = federation_one_enabled
			fail_text = no_federation_one
		}
		OR = {
			has_federation_type = research_federation
			custom_tooltip = {
				fail_text = has_fed_cent_high_or_higher_and_ethical_partners
				OR = {
					has_federation_law = centralization_high
					has_federation_law = centralization_very_high
				}
				has_federation_law = treaties_separate_ethical_partners
			}
		}
		# OR = {
		# 	custom_tooltip = {
		# 		fail_text = has_fed_cent_med_or_higher_and_research_federation
		# 		has_federation_type = research_federation
		# 		OR = {
		# 			has_federation_law = centralization_medium
		# 			has_federation_law = centralization_high
		# 			has_federation_law = centralization_very_high
		# 		}
		# 	}
		# 	custom_tooltip = {
		# 		fail_text = has_fed_cent_high_or_higher_and_ethical_partners
		# 		OR = {
		# 			has_federation_law = centralization_high
		# 			has_federation_law = centralization_very_high
		# 		}
		# 		has_federation_law = treaties_separate_ethical_partners
		# 	}
		# }
	}
	required_centralization = {
		base = 2
	}
	modifier = {}
	on_enact = {
		add_cohesion = @cohesion_loss
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_law_category_aof_market_cooldown
				days = @cooldown
			}
		}
		every_member = {
			remove_market_modifier = yes
			add_modifier = {
				modifier = f93_aof_market_strategic
				mult = value:f93_market_policy_strategic
			}
			country_event = { id = f93_aof_market.1 }
		}
	}
	ai_weight = { 
		base = 0
		
		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 31
		}
		modifier = {
			desc = federation_acceptance_materialist
			add = 20
			is_materialist = yes
		}
		modifier = {
			desc = federation_acceptance_technocracy
			add = 10
			has_civic = civic_technocracy
		}
		modifier = {
			desc = federation_acceptance_erudite_explorers
			add = 15
			has_ai_personality = erudite_explorers
		}
		modifier = {
			desc = federation_acceptance_peaceful_traders
			add = 10
			has_ai_personality = peaceful_traders
		}
	}
}
fed_market_military = {
	potential = {}
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = fed_market_military }
					has_modifier = federation_law_category_aof_market_cooldown
					has_any_federation_law_in_category = law_category_aof_market
				}
			}
		}
		custom_tooltip = {
			has_federation_flag = federation_one_enabled
			fail_text = no_federation_one
		}
		OR = {
			has_federation_type = military_federation
			custom_tooltip = {
				fail_text = has_fed_cent_very_high
				has_federation_law = centralization_very_high
			}
		}
		# OR = {
		# 	custom_tooltip = {
		# 		fail_text = has_fed_cent_med_or_higher_and_military_federation
		# 		has_federation_type = military_federation
		# 		OR = {
		# 			has_federation_law = centralization_medium
		# 			has_federation_law = centralization_high
		# 			has_federation_law = centralization_very_high
		# 		}
		# 	}
		# 	custom_tooltip = {
		# 		fail_text = has_fed_cent_very_high
		# 		has_federation_law = centralization_very_high
		# 	}
		# }
	}
	required_centralization = {
		base = 3
	}
	modifier = {}
	on_enact = {
		add_cohesion = @cohesion_loss
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_law_category_aof_market_cooldown
				days = @cooldown
			}
		}
		every_member = {
			remove_market_modifier = yes
			add_modifier = {
				modifier = f93_aof_market_military
				mult = value:f93_market_policy_military
			}
			country_event = { id = f93_aof_market.1 }
		}
	}
	
	ai_weight = { 
		base = 0
		
		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 31
		}
		modifier = {
			desc = federation_acceptance_militarist
			add = 20
			is_militarist = yes
		}
		modifier = {
			desc = federation_acceptance_authoritarian
			add = 20
			is_authoritarian = yes
		}
		modifier = {
			desc = federation_acceptance_peaceful_traders
			add = -5
			has_ai_personality = peaceful_traders
		}
		modifier = {
			desc = federation_acceptance_evangelising_zealots
			add = 10
			has_ai_personality = evangelising_zealots
		}
	}
}
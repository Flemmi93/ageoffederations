@cooldown = 3600
@cohesion_loss = -20

vote_weight_equal = {
	potential = {
		NOT = { has_federation_type = imperial_loyalists_federation }
	}
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = vote_weight_equal }
					has_modifier = federation_law_category_voting_weight_cooldown
					has_any_federation_law_in_category = law_category_voting_weight
				}
			}
		}
	}
	# modded content
	modifier = {
		federation_experience_add = 2
	}
	###
	on_enact = {
		set_equal_voting_power = yes
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_law_category_voting_weight_cooldown
				days = @cooldown
			}
		}
		# modded content / edit
		add_cohesion = @cohesion_loss # original value -10
		###
	}

	ai_weight = {
		base = 0

		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 51
		}
	}
}

vote_weight_diplomatic = {
	potential = {
		NOT = { has_federation_type = imperial_loyalists_federation }
		host_has_dlc = Federations
	}
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = vote_weight_diplomatic }
					has_modifier = federation_law_category_voting_weight_cooldown
					has_any_federation_law_in_category = law_category_voting_weight
				}
			}
		}
		NOR = {
			has_federation_law = diplomatic_bloc_common_voice
			has_federation_law = diplomatic_bloc_clear_voice
			has_federation_law = diplomatic_bloc_united
			has_federation_law = centralization_high
			has_federation_law = centralization_very_high
		}
	}
	required_centralization = {
		# base = 3
	}
	on_enact = {
		set_equal_voting_power = no
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_law_category_voting_weight_cooldown
				days = @cooldown
			}
		}
		# modded content / edit
		add_cohesion = @cohesion_loss # original value -10
		###
	}

	ai_weight = {
		base = 0

		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 26
		}
	}
}